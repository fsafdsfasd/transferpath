/**
 * Planner UX cap for “credits toward transfer” bars and checklist copy.
 * Many schools publish higher totals (e.g. 60); this is the app’s planning default only.
 */
export const PLANNER_CREDIT_TARGET = 30
